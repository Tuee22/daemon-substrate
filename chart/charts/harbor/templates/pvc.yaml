apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-harbor-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: daemon-substrate-manual
  volumeName: daemon-substrate-harbor
  resources:
    requests:
      storage: 10Gi
