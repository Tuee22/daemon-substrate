apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-harbor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: harbor
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: registry
      port: 5000
      targetPort: registry
