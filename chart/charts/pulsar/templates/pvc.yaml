apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-pulsar-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: daemon-substrate-manual
  volumeName: daemon-substrate-pulsar
  resources:
    requests:
      storage: 10Gi
